import React from 'react';
import { MainInstrutions } from './mainInstrutions';

export const Assembly: React.FC = () => {
  return (
    <div style={{ background: '#050608' }}>
      <MainInstrutions />
    </div>
  );
};
